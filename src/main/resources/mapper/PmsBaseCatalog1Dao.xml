<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.PmsBaseCatalog1Dao">

    <resultMap type="com.example.demo.entity.PmsBaseCatalog1" id="PmsBaseCatalog1Map">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PmsBaseCatalog1Map">
        select
          id, name
        from gmall.pms_base_catalog1
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PmsBaseCatalog1Map">
        select
          id, name
        from gmall.pms_base_catalog1
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PmsBaseCatalog1Map">
        select
          id, name
        from gmall.pms_base_catalog1
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into gmall.pms_base_catalog1(name)
        values (#{name})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update gmall.pms_base_catalog1
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from gmall.pms_base_catalog1 where id = #{id}
    </delete>

</mapper>